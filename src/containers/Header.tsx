import React from 'react';
import { observer } from 'mobx-react-lite';

const Header = () => {
    return (
        <div>
            I'm the header
        </div>
    )
}

export default observer(Header);