import React from 'react';
import { observer } from 'mobx-react-lite';

const Screen = () => {
    return (
        <div>
            I'm the Screen
        </div>
    )
}

export default observer(Screen);